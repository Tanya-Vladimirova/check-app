services:
  db:
    image: postgres:15
    container_name: health_check_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: health_db
    ports:
      - "5432:5432"
    restart: always